# tools/ — 本地工具链

> 与 `repos/`（研究对象，只读）不同，这里是**工具**。工具仓库同样不做源码修改，
> 使用问题通过临时目录 + 路径映射解决。

| 目录名 | 来源 | commit | 状态 |
|---|---|---|---|
| （暂无） | | | |

## 说明

- Graphviz 已安装：`D:\Software\Graphviz-16.0.0-win64`（用户手工安装）。注意：会话启动后新装的 PATH 不生效，必要时用全路径 `...\bin\dot.exe`。
